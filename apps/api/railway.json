{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm run build --workspace apps/api && node apps/api/dist/server.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100
  }
}
